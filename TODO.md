# Cleanup TODO

Things noticed while building, parked rather than fixed. Nothing here is breaking the
site — it all works as of 65be9ff. Roughly in the order worth doing.

## 1. The selection dashboard describes a data layout that does not exist

`content/glider-selection.qmd`, in the "How the data behind this is organised" panel, tells
readers the page reads `gliders_cleaned.csv` plus `payloads.csv`, `integrations.csv`,
`rodeo.csv` and `links.csv`. Four of those were never created. `gliders_cleaned.csv` exists
at the repo root but nothing reads it — the page carries its glider rows inline in JS, and
its sensor and PAM data comes from `content/data/sensors.json`.

That text is live and wrong about the page it is on. Either describe what the page actually
does (the `__source` / `__confidence` / `__checked` convention in it *is* real), or relabel
the panel as a roadmap.

## 2. Orphans from the first dashboard

- `gliders_cleaned.csv` (repo root) — fed the original OJS dashboard, now unread.
- `kasey_testDashboard.qmd` — superseded by `content/glider-selection.qmd`.

Both still render and publish. Decide whether they stay as history or go.

## 3. The 3D view draws dives over land

`content/study-design.qmd`, "Follow the seafloor": where the route crosses land or water
shallower than the clearance, the dive is floored at 15 m and the point counted as *too
shallow to dive* (1,752 of them on the example area) — but the track is still drawn there.
Consistent with the page's existing warning that transects are not routed around land, but
it makes the dive count optimistic over a shelf. Rendering those stretches as surface
transit instead would be more honest.

## 4. Provenance of `content/glider-selection.qmd`

It is hand-maintained by several people now. Worth stating that in the file, because its
history says otherwise: it was first generated by a script (`build_page.py`) that was never
committed and lives only in a Claude session scratchpad. Nothing in the repo can regenerate
it wholesale — `code/build_sensor_data.py` only rewrites between the `SENSOR-DATA` markers
and refuses if they are missing — so this is documentation, not a live hazard.

Also unused: the per-glider `bp:` paths in the `GLIDERS` array. Best-practices links now go
to that site's front page, so those strings no longer build a URL.

## 5. README leftovers

- `## How to Cite` and `## Funding` are still TODO.
- Collaborator list says "Kasey Costello"; the email is `kcastello@ucsd.edu` and the GitHub
  account is `KaseyMCastello`. Probably Castello — worth confirming with them rather than
  guessing.

## 6. One figure to confirm

`content/data/extra-sensors.csv`: `JASCO OceanObserver (1 channel)` carries 1.6 Ah/day,
which is the Slocum Endurance Tool's *generic hydrophone* row, not a measured OceanObserver
draw. The 5-channel row (6.4 Ah/day) is measured, from capex987 at the Rodeo. Worth
confirming the single-channel number against something real; on the page's defaults it is
the difference between 75 and 45 days.
