ReadMe for Output Folder

This folder is for modified or intermediate data products made by the code in `code/`
(for example per-mission-mode summary tables). Keep outputs small; large products are
gitignored and should be stored elsewhere.
